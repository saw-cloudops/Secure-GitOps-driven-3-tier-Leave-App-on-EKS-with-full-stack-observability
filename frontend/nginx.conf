server {
    listen 80;
    
    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # Prometheus Nginx Exporter scans this
    location /metrics {
        stub_status;
        allow all; 
    }
}
