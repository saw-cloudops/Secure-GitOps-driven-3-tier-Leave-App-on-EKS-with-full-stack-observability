docker run -d \
  --name mysql \
  -e MYSQL_ROOT_PASSWORD=root \
  -e MYSQL_DATABASE=leave_db \
  -p 3306:3306 \
  mysql:8


docker exec -it mysql mysql -u root -proot leave_db

docker exec -it mysql mysql -uroot -proot leave_db -e "UPDATE users SET role='ADMIN' WHERE username='yourusername';"
